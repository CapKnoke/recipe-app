import React from 'react';
import RecipeDetails from '../../components/RecipeDetails';

const Random = () => {
  return (
    <>
      <RecipeDetails random={true} />
    </>
  )
};

export default Random;
