import React from 'react';
import RecipeDetails from '../../components/RecipeDetails';

const Recipe = () => {
  return (
    <>
      <RecipeDetails random={false} />
    </>
  )
};

export default Recipe;
